# Action Required: Add Nano Banana Pro Model

No manual steps required for this feature.

`KIE_API_KEY` đã tồn tại trong `.env` và được share với 2 model hiện có — Nano Banana Pro dùng chung key. Không cần account mới, env var mới, hay third-party service setup.

All tasks can be implemented automatically.
