# Action Required: GPT Image-2 Model Selector

No manual steps required for this feature.

All tasks can be implemented automatically.

> `KIE_API_KEY` đã có trong `.env` và hoạt động cho cả 2 models — không cần tạo thêm API key mới.
