# Action Required: Swap GPT Image-2 to Image-to-Image variant

No manual steps required for this feature.

All tasks can be implemented automatically.

> `KIE_API_KEY` đã có trong `.env` và hoạt động cho tất cả models Kie AI — không cần tạo key mới. Dev server chạy ở port 3001 (`npm run dev -- -p 3001`).
